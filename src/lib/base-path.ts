// Single source of truth for the GitHub Pages sub-path (also used by next.config.ts).
// next/link and next/image prepend this automatically; raw <img>/<a> tags pointing
// at files in `public/` need it added manually.
export const basePath = "/eurasiaee";
